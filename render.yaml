services:
  - type: web
    name: verifund-api
    runtime: node
    plan: starter
    buildCommand: npm install && npm run db:generate && npm run db:push
    startCommand: npm run api
    envVars:
      - key: NODE_ENV
        value: production
